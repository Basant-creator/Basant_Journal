Phase 2 placeholder — no raster assets are in the critical path yet.
The survey sheet is generated SVG (lib/map/terrain.ts); textures are inline
data URIs in app/globals.css. This directory is reserved for Phase 3.
